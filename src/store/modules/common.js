export const state = {
  loading: true,
}
export const mutations = {
  ON_LOADING(state) {
    state.loading = true
  },
  OFF_LOADING(state) {
    state.loading = false
  },
}
export const actions = {}
